#include "ling.h"

void ling_get_widget_pos(GtkWidget * widget){
    // 获取控件相对于顶层窗口的位置
    // GtkWidget *toplevel = gtk_widget_get_toplevel(widget);
    // int x, y;
    // gtk_widget_translate_coordinates(widget, toplevel, 0, 0, &x, &y);
}
